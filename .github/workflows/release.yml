name: Release Automation

on:
  push:
    tags:
      - 'v*'
      - 'V*'
  workflow_dispatch:

jobs:
  release:
    name: Build & Publish Release
    runs-on: windows-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'
          cache: 'gradle'

      - name: Install Inno Setup
        run: choco install innosetup --no-progress

      - name: Build Distributions
        run: .\gradlew.bat :packages:portable:preparePortableBuild :packages:installer:prepareInstaller --no-daemon

      - name: Package Portable Zip
        run: |
          $dist = "packages/portable/build/portable/wllauncher"
          $zip = "WlLauncher-Portable.zip"
          Compress-Archive -Path "$dist/*" -DestinationPath $zip -Force

      - name: Compile Setup Installer
        run: |
          & "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" "packages/installer/build/innosetup/wllauncher/main.iss" /O"."

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            WlLauncher_Setup.exe
            WlLauncher-Portable.zip
          draft: false
          prerelease: false
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

